\section{Project Structure}

The system is implemented using Akka, where every replica and every client is an actor identified by a unique integer id. The project is organised around two abstract base classes, provided by the codebase, that fix the external API and the mandatory test callbacks, and two concrete subclasses that contain all of our protocol logic.
